PORT=5000
NODE_ENV=development
REDIS_URL="redis://127.0.0.1:6379"
RUN_WORKERS="false"

# Set to true to enable the Soroban contract event watcher.
# Requires CONTRACT_ID to be set — the server will refuse to start without it.
# Leave false (default) for REST-only development that does not need live blockchain events.
RUN_CONTRACT_WATCHER="false"

# Optional: protect GET /metrics (x-metrics-api-key or Authorization: Bearer)
METRICS_API_KEY=""

# PostgreSQL connection string
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/agrocylo_db"

# Supabase project connection
SUPABASE_URL="https://YOUR_PROJECT_REF.supabase.co"
SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"
SUPABASE_SERVICE_ROLE_KEY="YOUR_SUPABASE_SERVICE_ROLE_KEY"
SUPABASE_PRODUCT_IMAGES_BUCKET="product-images"
PRODUCT_IMAGE_PLACEHOLDER_URL="https://placehold.co/800x800/png?text=No+Image"

# Auth/JWT config used for RLS wallet policies
SUPABASE_JWT_SECRET="YOUR_SUPABASE_JWT_SECRET"

# JWT signing secret (REQUIRED in production, must be at least 32 characters)
# Generate with: openssl rand -base64 32
# Development default: dev-secret-change-in-production
JWT_SECRET="YOUR_JWT_SECRET_MIN_32_CHARS"

# Stellar Soroban contract watcher
CONTRACT_ID="YOUR_SOROBAN_CONTRACT_ID"
RPC_URL="https://soroban-testnet.stellar.org"

# WebSocket server path (clients connect to ws://host:PORT/ws)
WS_PATH="/ws"
